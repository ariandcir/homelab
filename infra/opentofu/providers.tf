# Intentionally empty provider configuration.
#
# This skeleton keeps module interfaces provider-agnostic so teams can either:
# 1) implement provider-specific modules in a separate folder/repo, or
# 2) execute manual provisioning from generated plans/specs when no provider API exists.
