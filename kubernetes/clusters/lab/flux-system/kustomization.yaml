apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Generated by `flux bootstrap` during cluster onboarding.
  # Keep these files committed in this directory for reproducible reconciles.
  # - gotk-components.yaml
  # - gotk-sync.yaml
  - namespace.yaml
