# Scripts

Operational scripts for local validation and controlled automation.

- `check-no-plaintext-secrets.sh` - detects unsafe secret anti-patterns.

> TODO(ops): add `bootstrap-flux.sh` and `render-omni-cluster.sh`.
